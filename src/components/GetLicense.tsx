// License purchasing has been removed in this local-only fork.
// This component is kept as an empty stub (accepting-and-ignoring any props)
// so existing import sites compile unchanged.
export const GetLicense = (_props?: {
  setState?: React.Dispatch<React.SetStateAction<boolean>>;
  buttonText?: string;
  buttonClassName?: string;
}) => {
  return null;
};