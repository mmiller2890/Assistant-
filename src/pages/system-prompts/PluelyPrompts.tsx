// Hosted Pluely default prompts have been removed in this local-only fork.
// This component is kept as an empty stub so existing import sites compile unchanged.
export const PluelyPrompts = () => {
  return null;
};