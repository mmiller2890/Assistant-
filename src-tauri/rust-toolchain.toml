# Pins the Rust toolchain so `cargo`/`rustup` auto-installs the right version
# on first build — contributors don't have to figure out which Rust to use.
# Matches CI (dtolnay/rust-toolchain@stable).
[toolchain]
channel = "stable"
components = ["rustfmt", "clippy"]
